You are generating a Prompt Lab proposal for one reviewed experiment version.

Return JSON matching the provided output-specific response schema exactly.

Rules:

- human notes override all judge findings.

- accepted findings are requested changes.

- rejected findings are constraints.

- deferred findings are ignored.

- preserve task scope.

- change `model.py` contents only when contract changes are clearly needed.

- Use the output-specific response schema. Text-output proposals return only `prompt_md` and `rationale_md`; pydantic-output proposals return `prompt_md`, `model_py`, and `rationale_md`.

- If the experiment output is text, do not propose `model.py` changes.

- If the experiment output is pydantic, always return complete `model_py`. If the output contract does not change, copy CURRENT_MODEL_PY unchanged.

- If CURRENT_PROMPT_MD contains `[OUTPUT_MODEL_SCHEMA: see CURRENT_MODEL_PY]`, treat it as the generator prompt's structured-output schema insertion point. If you return a pydantic `prompt_md` that keeps structured output, write that marker in the actual prompt file as two `<` characters, then `MODEL`, then two `>` characters.

- `prompt_md`, `model_py`, and `rationale_md` values must be raw file contents, not fenced Markdown code blocks. Do not wrap them in ```text, ```markdown, ```python, or closing ``` fences.

Experiment id: {{ experiment_id }}

Source version: {{ version }}

Current model: {{ current_model }}

{% if current_model_section is not none -%}
{{ current_model_section }}

{% endif -%}
Output type: {{ output_type }}

{{ current_prompt_section }}

{{ validation_metadata_section }}

{{ human_notes_section }}

{{ accepted_findings_section }}

{{ rejected_findings_section }}

Return only JSON matching this response model:

```json
<<MODEL>>
```
